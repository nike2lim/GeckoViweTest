# qa 작업 로그
- **일시**: 2026-08-06 09:15:51
- **작업**: 신규/변경 로직 자동 테스트 생성·실행 + requirements.md rev.3 기준 실기기 수용 기준 검증 (파이프라인 6단계)
- **입력**: `pipeline/requirements.md` rev.3, `pipeline/plan.md` rev.2, `pipeline/evaluation.md`, `pipeline/review.md` rev.1 §9 + rev.2 §17, `pipeline/impact-report.json`, 규칙 5종(verification-honesty / comment-style / architecture / scope-guard / work-logging), `doc/` 로그 11개
- **산출물**:
  - `pipeline/qa-report.md` (신규)
  - `app/src/test/java/com/example/geckoviewtest/bridge/BridgeWireContractTest.kt` (신규, 7건)
  - `app/src/test/java/com/example/geckoviewtest/bridge/ExtensionManifestTest.kt` (신규, 8건)
  - `app/src/test/java/com/example/geckoviewtest/bridge/BridgeProtocolTest.kt` (케이스 1건 추가)
  - `app/src/test/java/com/example/geckoviewtest/bridge/BridgeDispatcherTest.kt` (케이스 1건 추가 + MINOR-10 KDoc 정정)
  - 이 로그

## 수행 내용

### 1. 베이스라인 직접 재측정 (V7)
인계값(31건 / LINE 98/98 / BRANCH 16/18 / CLASS 14)을 인용하지 않고 `--rerun-tasks`로 강제 실행해 재측정했다. **전 항목 일치 — 인계값에 오류 없음.**

### 2. BRANCH 미커버 2개의 정체 규명
JaCoCo XML을 파싱해 위치를 특정했다: `BridgeProtocol.kt:67`의 `require(request.name.isNotBlank())`. 도달 불가능한 생성 코드가 **아니라** 실제로 탈 수 있는 경로였고(빈 name 요청), 인접한 `:65`·`:66`은 이미 양방향 커버돼 있었다. 테스트 1건으로 덮어 **BRANCH 18/18 = 100%** 달성.

### 3. 커버리지가 닿지 않는 영역의 자동화 (이 배치의 핵심)
LINE이 이미 100%였으므로 "수치를 올리는 테스트"가 아니라 **자동 통제가 0이던 곳**을 찾았다.
- **코틀린↔JS 계약**(`BridgeWireContractTest` 7건): 함수명 집합 양방향, `nativeApp` 문자열, `ErrorCode` 집합, 송신자 분기 부재(AC-003-3), 오리진 훅 전체 허용(H-1), JSON 문자열 전송(§3.1). 상수는 **리플렉션으로 수집**해 상수가 늘면 테스트가 따라가도록 했다.
- **manifest.json 불변식**(`ExtensionManifestTest` 8건): `page-bridge.js`의 `web_accessible_resources` 등록(§2.6.1 거짓 그린 최대 위험), MV2 고정, 권한 집합, `matches`, `all_frames`, 확장 id/version, background 스크립트명.

### 4. V2 — RED 증명 17건 전건
각 실험이 **하나의 테스트만** 빨갛게 만들도록 설계했고(부수 실패 전건 0), 실험 후 백업본 복원 + sha256 대조로 원복을 확인했다.
**실험 방법 자체의 함정을 먼저 잡았다**: 자산만 바꾸면 `:app:testDebugUnitTest`가 UP-TO-DATE로 건너뛴다는 것을 실측하고, 모든 RED 실험을 `--rerun`으로 강제 실행했다.

### 5. 실기기 수용 기준 검증 (rev.3 기준, API 33)
`ro.build.version.sdk = 33` 기록(AC-011-3 (d)). REQ-001~011 전부 검증. 시퀀스는 AC-010-5(앱 종료)를 **맨 마지막**에 두었고, 네트워크를 끈 뒤 **반드시 복구**했다.
특기할 성과: `remoteDebuggingEnabled(BuildConfig.DEBUG)`가 켜져 있는 것을 이용해 기기의 firefox-debugger-socket에 접속, **naver.com 페이지 세계에서 직접 JS를 평가**했다. 프로덕션을 고치지 않고 `window.NativeBridge` 존재·`__bridgeProbeRanInPageWorld === true`·**살아 있는 5단 왕복(`OK:"1.0.0"`)**·미지원 함수 reject·`appFinish`에 의한 실제 종료를 전부 실측했다.

## 결정 사항

1. **계측 테스트(`androidTest`)를 추가하지 않았다.** 러너 의존성(`androidx.test:runner`·`ext:junit`)이 plan.md §6.1 승인 목록 밖이라 evaluator 재승인이 필요하고(V9), CI·에뮬레이터 경로가 없어 **V4가 요구하는 "필터가 실제로 작동함"을 케이스 수 차이로 증명할 수 없다.** 증명 못 하는 환경 한정 표식은 가장 위험한 거짓 그린이므로 붙이지 않았다. 후속 과제로 넘겼다.
2. **JS 런타임 테스트를 만들지 않았다.** Node/Jest + `browser.*` 목이 필요한데 승인 밖 의존성이고, 목 기반 초록은 #220 같은 "GeckoView 구현에서만 깨지는" 결함을 원리적으로 잡지 못한다. 대신 **조용히 실패하는 지점만 정적으로** 덮었다.
3. **재수행하지 않은 수용 기준 3건**(AC-003-2 역주입 / AC-004-3 versionName 변경 / AC-010-4 제2 사이트): 셋 다 프로덕션 파일 일시 수정 + 재빌드가 필요하다. *"프로덕션 코드를 수정하지 마라"* 지시를 우선해 수행하지 않았고, **"통과"로 적지 않고 "관측하지 않았다"로 기록**했다. AC-010-4는 앱에 URL 입력 UI도 `VIEW` 인텐트 필터도 없고 네이버 외부 링크는 `onNewSession`(스코프 아웃)이라 이동하지 않음을 실측 확인했다. 대신 **M-5가 그 위험(naver 하드코딩된 matches)을 상시 자동 차단**하도록 했다.
4. **AC-009-4를 두 반쪽으로 나눠 판정**했다. "사라진다"는 39개 샘플로 직접 관측(PASS). "뜬 뒤"는 **5회 중 0회** 포착이며, 원인을 추정하지 않고 측정했다 — `PageStart`→`PageStop` 간격이 **221 ms**로 `uiautomator dump` 1주기(약 700 ms)보다 짧다. 앱 결함이 아니라 계측 한계(V3)로 분류했다.
5. **MINOR-10은 처리, MINOR-11은 미처리.** 전자는 테스트 파일이라 손댈 수 있었고, 후자는 프로덕션 주석이라 두었다.

## 이슈 / 리스크

1. **[신규 발견 — 후속 과제] 자산만 수정하면 단위 테스트가 UP-TO-DATE로 건너뛴다.** 새로 만든 15건이 `src/main/assets/messaging/**`를 직접 읽는데 그 경로가 태스크 입력으로 선언돼 있지 않다. 실측으로 재현했다. 클린/CI/`--rerun-tasks`에서는 정상 실행되므로 이 리포트의 수치는 영향받지 않는다. 해결책(`inputs.dir(...)` 한 줄)은 빌드 스크립트 수정이라 이번 범위 밖 — qa-report §9-4에 실행 가능한 형태로 넘겼다.
2. **AC-011-3의 API 30 이하 갈래는 미실측**이다(해당 기기 없음). `minSdk = 26`이라 지원 범위 안이다.
3. **`background.js`의 mtime이 갱신됐으나 내용은 동일**하다(sha256 대조 확인). RED 실험 후 백업 복원 때문이며, **트랩 #10이 이번에도 재현**됐다. 후속 단계는 mtime이 아니라 내용으로 변경을 판정할 것.
4. **범위 밖 확인 사항(결함 아님)**: 접근성 트리가 GeckoView를 `android.webkit.WebView`로 보고한다(호환 매핑 — dex·소스 실사용 0건). 네이버 외부 링크 미동작(`onNewSession` 스코프 아웃). `PermissionDelegate` 미구현 경고. 이월 2건(MINOR-1·MINOR-8). 전부 결함으로 올리지 않았다(V9).
5. **팀 리드의 재개 지시에 사실 오류가 있었다.** *"산출물이 하나도 생성되지 않았다 / `app/src/test/`는 기존 4개 파일 그대로"* 라고 전달받았으나, 실제로는 신규 테스트 2개 파일과 기존 2개 파일 수정이 디스크에 남아 있었고 프로덕션 파일도 정상 원복돼 있었다. 파일 시스템을 직접 확인해 검증한 뒤 **처음부터 다시 하지 않고 이어서** 진행했다. 실제로 없었던 것은 `qa-report.md`와 이 로그뿐이다.

## 판정

**PASS**

- **프로덕션 결함 0건.** REQ-001~011 전부 requirements.md rev.3 기준 충족.
- 테스트 48건 / 실패 0 / skip 0 / `@Ignore` 0건.
- LINE 98/98 = 100.0% (유지), **BRANCH 16/18 → 18/18 = 100.0%**, CLASS 14/14 (유지). 회귀 0건.
- 화이트리스트 위반 0건 — 변경 파일 전부 `app/src/test/**`.
- 미관측으로 남긴 것(AC-003-2·AC-004-3·AC-010-4, AC-011-3의 API 30 이하 갈래)은 통과로 적지 않고 사유와 함께 명시했다.
