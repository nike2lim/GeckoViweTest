# code-reviewer 작업 로그 (재리뷰 — developer의 CHANGES_REQUESTED 대응 검증)

- **일시**: 2026-08-05 13:51:02
- **작업**: 파이프라인 5단계 **재리뷰** — 1차 리뷰(`20260805_133319_code-reviewer.md`)의 MAJOR-1 해소 여부 판정 및 회귀 검증
- **입력**: `doc/20260805/20260805_134145_developer.md`(재작업 로그), `pipeline/requirements.md` **rev.3**, `doc/20260805/20260805_134039_planner-analyzer.md`, 1차 리뷰 `pipeline/review.md` rev.1 + 내 1차 로그, 규칙 5종
- **산출물**: `pipeline/review.md` **rev.2 절 추가**(rev.1 본문 보존, 상단에 개정 이력 신설), 본 로그
- **판정**: **APPROVED**

> 1차 로그(`20260805_133319_code-reviewer.md`)는 **수정하지 않았다.** 이 파일은 새 실행 1회분이다.

---

## 수행 내용

### 1. 변경 파악 — mtime으로 실제 변경 파일을 특정했다 (git 저장소 아님)

1차 리뷰 종료 시각(13:34) 이후 mtime이 갱신된 파일을 hook의 fnmatch 로직과 함께 조회한 결과 **8개**가 나왔다. developer가 로그에 열거한 것은 **7개**다.

8번째는 `bridge/BridgeDispatcher.kt`(13:40:24)였고, **MINOR-5의 RED 실험 대상**(`runCatching{}.getOrDefault("SWALLOWED")`)이었다. 백업 복원으로 mtime만 갱신된 것인지 실제 미보고 변경인지 구분해야 했으므로 **파일 전문을 1차 리뷰 시점과 대조**했다 — **내용 동일**이었다. 원복은 완전하다.

전 8개 파일 **ALLOW**. 전체 42개 중 화이트리스트 밖은 여전히 `.claude/settings.json`·`initRequire.md` 2개뿐이며 둘 다 착수 전 파일로 이번에도 변경되지 않았다.

### 2. MAJOR-1 — 두 주석을 직접 읽어 3요소 대조

`MainActivity.kt:38-63`과 `AppNavigationDelegate.kt:10-27`을 읽고 1차 리뷰가 요구한 ①플랫폼 기본 동작 ②API 31+ vs API 30 이하 분기 ③`appFinish`가 확실한 종료 경로 — **세 요소 모두 두 파일에 있음**을 확인했다.

**정직성을 별도로 확인했다.** 실측값(`state=STOPPED stopped=true finishing=false`)에는 "실측: API 33 기기에서" 출처가 붙어 있고 내 실험 A 관측값과 문자 단위로 일치한다. 반대로 **API 30 이하 동작에는 "이번 검증에 실기기가 없어 문서상 동작이며 실측하지 않았다"는 한계가 두 파일 모두에 붙어 있다.** 없는 관측을 주장하지 않았다(V7).

### 3. 재측정 — developer 보고를 인용하지 않고 다시 실행

`clean` + `--rerun-tasks`로 캐시를 배제하고 강제 실행했다.

```
./gradlew clean assembleDebug jvmCoverageReport --rerun-tasks
→ BUILD SUCCESSFUL (^e: 0건, ^w: 0건)
→ LINE 98/98 = 100.0% · BRANCH 16/18 = 88.9% · CLASS 14/14
→ 테스트 31건, 실패 0, skip 0
```

1차 리뷰 §9-8에서 내가 제시한 베이스라인과 **정확히 동일**하다. 회귀 없음.

### 4. 실기기 회귀 — MINOR-2가 유일한 프로덕션 로직 변경이므로 직접 태웠다

`NativeBridgeHandler`는 커버리지 제외 대상이라 단위 테스트가 닿지 않는다. 새 APK를 `install -r`로 재설치하고 force-stop 후 재기동해 **3단·5단 두 경로를 모두** 확인했다(한쪽 결과를 다른 쪽 근거로 쓰지 않기 위해).

- 3단: 화면 `versionName = 1.0.0` ↔ `dumpsys versionName=1.0.0` 문자 단위 일치
- 5단: naver.com 배지 `versionName = 1.0.0 [PAGE_WORLD]` + 페이지 콘텐츠 정상 렌더
- logcat: `NativeBridge: onMessage … javaType=java.lang.String` 2건(왕복 2회분), `브리지 처리 중 예외` 0건, 치명 오류 0건

**`catch (CancellationException) { throw e }` 추가가 브리지를 깨지 않았다.**

### 5. requirements.md rev.3 대조

내가 1차 리뷰 §1에서 후속 조치로 제시한 AC-011-3·AC-002-2 정정안이 실제로 반영됐는지 원문을 읽어 확인했다. **정정안대로 반영됐고 두 지점은 내 안보다 낫다**(§ 결정 사항 2 참조).

---

## 결정 사항 (선택지가 있었던 지점)

### 1. 신규 MINOR 2건을 이유로 다시 반려할 것인가 → **하지 않았다. APPROVED.**

재리뷰에서 MINOR 2건을 새로 찾았다.

- **MINOR-10** — `BridgeDispatcherTest.kt:16-17`의 클래스 KDoc "보장하는 것"이 아직 *"예외를 던졌을 때 … 오류 응답으로 바뀌는지"*라고 적혀 있는데, **이 스위트는 그것을 보장하지 않는다.** MINOR-5로 날카로워진 케이스 주석(L104-105)이 *"INTERNAL_ERROR 변환은 `NativeBridgeHandler`의 몫이며 여기서 검증하지 못한다"*고 정반대로 말한다. KDoc이 케이스 주석보다 넓게 단정해 T1이 부정확해졌다.
- **MINOR-11** — 두 주석의 *"(AC-011-3) … 문언 정정 대기 중"* 표현이 낡았다. rev.3이 13:38:42에 정정을 완료했다.

**반려하지 않은 이유**: 둘 다 주석 한 줄이고 **틀린 사실을 단정하지 않는다.** 1차의 MAJOR-1은 "플랫폼 동작이 X다"라고 **거짓을 단정**했지만, MINOR-10은 범위 과잉(바로 아래 케이스 주석이 정정하고 있다)이고 MINOR-11은 시점 지연이다. 어느 쪽도 거짓 그린을 만들지 않으며 테스트는 실제로 회귀를 잡는다(RED 재증명 확인). 재작업 루프 비용이 이익을 넘는다.

**MINOR-11은 developer의 부주의가 아니라는 점을 명시했다.** 주석 작성 시각(13:37:43·13:39:01)과 rev.3 발행 시각(13:38:42)이 **동시 진행**이었다. 타임스탬프를 확인하지 않고 "낡은 주석을 방치했다"고 적었다면 부당한 지적이 됐을 것이다.

### 2. AC-011-4 rev.3 문언이 V1을 여전히 만족하는가 → **만족한다. 오히려 강해졌다.**

팀 리드가 특별히 판정을 요구한 항목이다. rev.3은 판정 근거를 "종료 여부"에서 **"두 조작의 결과가 서로 다르다"**로 바꿨다. 두 실패 모드를 각각 대입해 확인했다.

- 콜백이 **항상 켜져 있으면**: index 뒤로가기가 히스토리 없는 `goBack()`을 불러 아무 일도 안 일어남 → 포그라운드 유지 → 두 결과가 같아짐 → **잡힌다.**
- 콜백이 **항상 꺼져 있으면**: naver 뒤로가기가 플랫폼 기본 동작을 타 `index.html`로 복귀 못 함 → AC-011-1이 깨지고 두 결과도 같아짐 → **잡힌다.**

두 항이 같은 조작 쌍에서 **반대 결과**로 정의돼 있어 어느 쪽도 "사실상 항상 참"이 아니다. 게다가 **rev.2 문언이 오히려 검증 불가였다** — AC-011-3의 "종료" 단정에 기대고 있었는데 그 단정이 API 31+ 에서 거짓이라 검증 기기에서 원리적으로 판정할 수 없었다. rev.3이 그 결함을 없앴다.

### 3. rev.3이 내 정정안보다 나아진 점을 인정할 것인가 → **인정하고 기록했다.**

rev.3은 Android 12 동작 변경이 **`targetSdk`가 아니라 기기 OS 버전으로 적용된다**는 사실을 [문서] 근거와 함께 명시했다. 나는 1차 리뷰에서 이 구분을 명시하지 않았다. 이것이 AC-011-3 (d)의 "targetSdk가 아니라 **기기 API**를 기록하라"는 요구의 논리적 근거가 되므로, 내 안을 그대로 옮긴 것이 아니라 **더 정확하게 만든 것**이다. review.md §15에 그렇게 적었다.

또 rev.3의 출처 표기가 모범적이다 — 실기기 관측값을 "재현하지 않고 인용 — 출처 명시"로 분류하고 *"planner-analyzer는 기기를 조작하지 않았다"*고 적었다. `dexdump` 전수 조사는 직접 재측정 [확인]으로 분리했다. **자기가 하지 않은 관측을 자기 것으로 적지 않았다.**

### 4. review.md를 덮어쓸 것인가 → **rev.1 본문을 보존하고 rev.2 절을 덧붙였다.**

상단에 개정 이력 표를 신설하고, 1차 리뷰 본문에 `# rev.1 — 1차 리뷰 … CHANGES_REQUESTED` 헤더를 씌워 시점을 명시했다. rev.1의 §1(AC-011-3·AC-002-2 수용 판정)은 **rev.3 정정의 근거 문서**이므로 지우면 왜 요구사항이 바뀌었는지 추적할 수 없게 된다.

---

## 이슈 / 리스크

1. **developer 로그의 파일 수가 두 배치 연속으로 어긋났다.** 직전 배치는 "소스 40개"(실제 39), 이번은 "수정한 7개 파일"(실제 mtime 갱신 8개). **이번 건은 결함이 아니다** — 8번째는 백업 복원으로 mtime만 바뀐 `BridgeDispatcher.kt`이고 내용 대조 결과 동일했다. 다만 **mtime만 보는 후속 단계가 "미보고 변경"으로 오인할 수 있어** QA 인계 4번으로 넘겼다(변경 판정은 내용 대조로 할 것).

2. **MINOR-10이 1차 리뷰에서 잡히지 않은 항목이다.** 1차에서는 테스트명이 모호해(`결과 타입은 유지된다`) KDoc의 과잉 단정이 드러나지 않았는데, MINOR-5로 케이스가 날카로워지면서 **KDoc이 뒤처진 것이 보이게 됐다.** 지적을 고치면 인접 주석이 낡는 전형적인 사례이며, 후속 권고로 넘겼다.

3. **다음 배치로 넘어간 2건은 그대로 유효하다** — MINOR-1(`ensureBuiltIn` 실패 시 미처리 코루틴 예외 → "확장 설치 실패의 사용자 대면 처리"), MINOR-8(프로브 배지 always-on → release 도입 선행 작업). developer가 손대지 않은 것은 내 권고와 V9에 따른 것이므로 **반려 사유로 쓰지 않았다.**

4. **기기 반납 상태** — `R3CN60L0QMT`(API 33)에 최신 APK 설치됨. 마지막 조작은 **naver.com을 띄운 채 포그라운드**(종료하지 않았다). 비행기모드 해제 확인(`airplane_mode_on = 0`). 리뷰 과정에서 **프로덕션 코드·테스트·설정 파일을 한 줄도 수정하지 않았다** — 산출물은 `pipeline/review.md` 갱신과 본 로그뿐이다.

5. **커버리지 베이스라인(coverage-reporter 인계)** — LINE **98/98 = 100.0%**, BRANCH **16/18 = 88.9%**, CLASS **14**. `clean` + `--rerun-tasks`로 재측정한 값이다.

---

## 판정

**APPROVED**

- **MAJOR 잔여 0건.** MAJOR-1이 요구된 3요소 + 미실측 한계 명시까지 담아 해소됐다.
- 처리 대상 MINOR 6건(2·4·5·6·7·9) 전부 반영. MINOR-3은 기록 의무 이행. MINOR-1·8은 정당한 이월.
- **재측정 회귀 0건** — 빌드 경고 0, 테스트 31/0/0, 커버리지 LINE 100.0% / BRANCH 88.9% 동일, 실기기 3단·5단 왕복 통과.
- **developer 보고와 불일치 없음**(파일 수 1건은 mtime 갱신분이며 결함 아님).
- `requirements.md` rev.3의 AC-011-3·AC-002-2 정정이 내 정정안대로 반영됐고, 판정 명령이 `dexdump`로 교체돼 "영원히 실패하는 기준"의 함정이 제거됐다. AC-011-4 재문언도 V1을 만족한다.
- 신규 MINOR 2건(10·11)은 주석 한 줄이며 **비차단**. 후속 권고로 넘긴다.

→ **6단계 QA로 진행 가능.**
