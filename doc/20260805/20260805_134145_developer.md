# developer 작업 로그 (재작업 — code-reviewer CHANGES_REQUESTED 대응)

- **일시**: 2026-08-05 13:41:45
- **작업**: 5단계 code-reviewer의 **MAJOR-1**(주석 2곳) 수정 + **MINOR-6** 처리 + 선택 MINOR 5건 처리
- **입력**: `pipeline/review.md`, `doc/20260805/20260805_133319_code-reviewer.md`, 직전 내 로그 `doc/20260805/20260805_131319_developer.md`
- **판정**: **PASS** — MAJOR-1 해소. 빌드·테스트·실기기 회귀 전부 통과. 커버리지 회귀 없음.

> 앞 로그(`20260805_131319_developer.md`)는 **수정하지 않았다.** 이 파일은 새 실행 1회분이다.

---

## 1. MAJOR-1 — 뒤로가기 기본 동작 주석 2곳 (필수, 완료)

리뷰어가 요구한 3요소(① 플랫폼 기본 동작 ② API 31+ vs API 30 이하 분기 ③ `appFinish`가 확실한 종료 경로)를 두 곳 모두에 넣었고, `(AC-011-3)` 참조는 유지하되 **문언 정정 대기 중**임을 명시했다. 독자가 Android 초보라는 기준에 맞춰 **"태스크 백그라운드 이동"을 "홈 버튼을 누른 것과 비슷하다"로 풀어썼다.**

### ① `app/src/main/java/com/example/geckoviewtest/MainActivity.kt` (backCallback KDoc)

```kotlin
/**
 * 뒤로가기 콜백. 이 콜백의 `isEnabled`가 뒤로가기 동작을 가른다:
 *  - `true`  → 콜백이 실행되어 웹 페이지를 한 칸 뒤로 보낸다
 *  - `false` → 콜백을 건너뛰고 **플랫폼 기본 동작**이 수행된다. **우리 코드는 여기서 끝난다.**
 *
 * ┌ `false`일 때 실제로 무슨 일이 일어나는가 — 안드로이드 버전에 따라 갈린다 ────────────┐
 * │ **API 31 이상**: Activity가 종료되지 않고 **태스크가 백그라운드로 밀려난다.**          │
 * │   (= 홈 버튼을 누른 것과 비슷하다. 앱은 화면에서 사라지지만 메모리에 살아 있고,        │
 * │    다시 실행하면 보던 화면이 그대로 나온다.)                                          │
 * │   실측: API 33 기기에서 `dumpsys activity activities`가                               │
 * │   `state=STOPPED stopped=true finishing=false` — 즉 **소멸하지 않았다.**              │
 * │ **API 30 이하**: 예전 동작이 그대로라 **Activity가 종료된다.**                        │
 * │   이 앱의 `minSdk`가 26이므로 지원 범위 안에 두 동작이 **모두** 들어 있다.            │
 * │   (API 30 이하 실기기는 이번 검증에 없어 문서상 동작이며 실측하지 않았다.)            │
 * └──────────────────────────────────────────────────────────────────────────────────────┘
 *
 * **앱을 확실히 끝내는 경로는 이쪽이 아니다.** 웹 페이지의 `appFinish` 호출이
 * [MainUiEvent.Finish] → [handleEvent]의 `finish()`로 이어지는 경로이며,
 * 그쪽은 버전과 무관하게 실제로 ActivityRecord가 소멸한다(실측: `dumpsys`에서 완전히 사라짐).
 *
 * (AC-011-3) — **다만 이 수용 기준의 문언은 "뒤로가기로 Activity가 종료된다"로 되어 있어
 * API 31 이상의 동작을 반영하지 못한다. 문언 정정 대기 중이며, 구현을 바꿀 사안이 아니다.**
 * 강제로 `finish()`를 부르면 오히려 플랫폼 표준 동작에서 이탈한다.
 *
 * 값을 정하는 것은 ViewModel이고([MainUiState.canGoBack]), 여기서는 [render]가 옮겨 담기만 한다.
 */
private lateinit var backCallback: OnBackPressedCallback
```

### ② `app/src/main/java/com/example/geckoviewtest/gecko/AppNavigationDelegate.kt` (클래스 KDoc 본문)

```kotlin
 * [GeckoSession.NavigationDelegate.onCanGoBack]은 "웹 히스토리에 뒤로 갈 곳이 있는가"가
 * 바뀔 때마다 불린다. 이 값이 뒤로가기 버튼의 동작을 가른다:
 *  - 갈 곳이 **있으면** → 뒤로가기 콜백이 켜져 페이지를 한 칸 뒤로 보낸다
 *  - 갈 곳이 **없으면** → 콜백이 꺼지고 **플랫폼 기본 동작**이 수행된다. 우리 코드는 여기서 끝난다.
 *
 * **"플랫폼 기본 동작"이 곧 앱 종료는 아니다 — 안드로이드 버전에 따라 갈린다:**
 *  - **API 31 이상**: Activity가 종료되지 않고 **태스크가 백그라운드로 밀려난다**
 *    (홈 버튼을 누른 것과 비슷하다. 화면에서는 사라지지만 Activity는 살아 있다).
 *    실측: API 33 기기에서 `dumpsys`가 `state=STOPPED finishing=false`.
 *  - **API 30 이하**: 예전 동작이라 **Activity가 종료된다.** 이 앱의 `minSdk`가 26이므로
 *    지원 범위 안에 두 동작이 모두 들어 있다(API 30 이하는 이번 검증에 실기기가 없어 미실측).
 *
 * 앱을 확실히 끝내는 경로는 웹 페이지의 `appFinish` → `MainUiEvent.Finish` → `finish()`이며,
 * 그쪽은 버전과 무관하게 ActivityRecord가 실제로 소멸한다.
 *
 * (AC-011-3) — **이 수용 기준의 문언은 API 31 이상의 동작을 반영하지 못해 정정 대기 중이다.**
 * 구현을 바꿀 사안이 아니다.
```

### V7 — 주석에 단정한 값의 출처

| 주석에 쓴 사실 | 출처 |
|:--|:--|
| `minSdk = 26` | **내가 직접 재확인** — `app/build.gradle.kts:26` |
| API 33에서 `state=STOPPED finishing=false` | **내가 직접 관측**(직전 배치) + 리뷰어 실험 A와 일치 |
| `appFinish` 경로에서 ActivityRecord 소멸 | **내가 직접 관측**(AC-005-1, grep 0건) + 리뷰어 실험 C와 일치 |
| **API 30 이하에서는 Activity가 종료된다** | **리뷰어가 지적한 사실이며, 나는 실기기로 확인하지 못했다.** 그래서 주석에 *"이번 검증에 실기기가 없어 미실측"*이라고 **한계를 함께 적었다.** 문서상 동작으로만 단정한다 |

---

## 2. MINOR-6 — compose 애너테이션 유입 경로 코드 기록 (권장, 완료)

`app/build.gradle.kts`의 `activity-ktx` 선언 바로 위에 넣었다.

```kotlin
// ⚠ 의존성 트리에서 `androidx.compose.runtime`을 보더라도 놀라지 말고 **exclude하지 말 것.**
//   유입 경로: activity 1.13.0 → androidx.navigationevent 1.0.0 → compose.runtime:runtime-annotation
//   이것은 **애너테이션 전용 아티팩트이고 Compose UI가 아니다.**
//   근거(실측): 빌드된 APK의 dex 7개를 전수 조사한 결과 androidx/compose 정의 클래스는 6개뿐이고
//   전부 애너테이션 타입 + R이다 — Stable / Immutable / StableMarker /
//   annotation.RememberInComposition / annotation.FrequentlyChangingValue / annotation.R.
//   compose.ui · foundation · material · runtime 본체는 0건이며 Compose 컴파일러도 적용되지 않았다.
//   따라서 REQ-002(XML View 사용, Compose 미사용) 위반이 아니다.
//   exclude하면 navigationevent가 깨질 수 있고, 얻는 것은 의존성 목록의 겉보기뿐이다.
implementation(libs.androidx.activity.ktx)
```

> dex 심볼 6개는 **내가 직접 조사한 값**(직전 배치, `strings` 기반)이고, 리뷰어가 `dexdump`로 독립 재확인해 **동일 결과**가 나왔다. 두 방법이 일치한다.

---

## 3. 처리한 선택 MINOR (4건)

| # | 파일 | 조치 | 판단 근거 |
|:--|:--|:--|:--|
| **MINOR-2** | `bridge/NativeBridgeHandler.kt` | `catch (e: CancellationException) { throw e }`를 `catch (e: Exception)` **앞에** 추가 + 근거 주석 | **실제 버그 성격**이다. 취소가 오류 응답으로 둔갑하면 스코프가 정리되지 않는다. 리뷰어 권고대로 좁히는 대신 재던지기를 택했다 — 취소 신호를 삼키지 않으면서 나머지 예외의 `INTERNAL_ERROR` 변환은 그대로 유지되기 때문이다 |
| **MINOR-5** | `test/.../BridgeDispatcherTest.kt` | 테스트명 → `Repository 예외는 삼켜지지 않고 그대로 전파된다`. Fake를 `version: String` + **`throws: Boolean` 플래그 분리** | 기존 `version = null`이 "던진다"의 트리거라 **프로덕션 의미(null → `UNKNOWN` 폴백)와 정반대**를 학습시켰다. 플래그를 분리하고 그 이유를 Fake KDoc에 적었다 |
| **MINOR-9** | `App.kt` | `by lazy` 근거를 정정 | 리뷰어 지적이 맞다. 첫 접근이 `MainActivity.onCreate`라 **비용은 회피되지 않고 옮겨질 뿐**이다. 진짜 이유(화면 없는 프로세스 시작에서는 엔진을 아예 안 만든다)로 다시 썼다 |
| **MINOR-7** | `.gitignore` | `build/`, `app/build/`, `.gradle/`, `.kotlin/`, `local.properties`, `.idea/`, `*.iml`, `.DS_Store` 추가 | `git init` 시 **198 MB APK가 커밋 대상**이 되는 것을 막는다. **기존 Claude Config Manager 규칙은 한 줄도 건드리지 않고 아래에 덧붙였다** |

### MINOR-4 — 주석으로 처리 (배선 이동 대신)
`MainActivity`의 `viewModels { Factory(NAVER_URL, Dispatchers.Default) }`가 `AppContainer`를 거치지 않는 것이 **의도임을 KDoc에 명시**했다: 두 값은 화면에 딸린 값이라 앱 스코프 컨테이너가 알 이유가 없고, D-11의 실질(생성자 주입 + 테스트 교체 가능)은 유지되며 `MainViewModelTest`가 실제로 둘 다 바꿔 넣어 검증한다. **배선을 옮기지 않은 이유**: 기능적 영향이 없는데 검증 완료된 배선을 흔들 이유가 없다.

### MINOR-3 — 코드 변경 없음, 이탈로 기록
plan D-06이 나열한 `MainUiState.lastBridgeResult` 필드를 **의도적으로 넣지 않았다.** 브리지 결과는 **웹 페이지가 자기 DOM에 표시**하므로(`index-page.js`의 `#result`, `page-bridge.js`의 배지) 안드로이드 `UiState`에 두면 아무도 읽지 않는 죽은 필드가 된다. 리뷰어도 *"판단 자체는 옳다"*고 확인했다. **직전 로그의 "계획과 다른 점"에 빠져 있었으므로 여기 기록한다.**

---

## 4. 손대지 않은 것 (V9 — 범위 밖. 다음 배치로 넘긴다)

| # | 내용 | 넘기는 이유 |
|:--|:--|:--|
| **MINOR-1** | `ensureBuiltIn` 실패 시 `bridgeExtension.await()`의 예외가 `lifecycleScope.launch`에서 **처리되지 않은 코루틴 예외로 앱을 죽인다.** `MainUiState`에 오류 표현이 없어 화면에 사유가 남지 않는다 | **계획이 규정하지 않은 경로**다. 고치려면 `MainUiState`에 오류 필드를 추가하고 `render()`·테스트까지 번져 범위가 넓어진다. **다음 배치 요구사항으로 넘긴다** — "확장 설치 실패의 사용자 대면 처리" |
| **MINOR-8** | 검증 프로브(고정 배지 + 모든 페이지 로드 시 자동 `getVersionName`)가 **빌드 타입과 무관하게 항상 실행**된다. requirements REQ-010은 *"debug 빌드 한정 권장"* | release가 스코프 아웃이라 이번 배치의 결함이 아니다. **release 도입 배치의 필수 선행 작업으로 기록한다** — 이 상태로 release를 내면 모든 방문 사이트 최상단에 검정 배지가 노출된다 |

**직전 배치에서 넘긴 것도 그대로 유효하다**: naver.com 로드 시 `GeckoConsole: Permission error: No listener for GeckoView:ContentPermission` (`PermissionDelegate` 미구현, requirements §6 스코프 아웃).

---

## 5. 검증 결과 — 실측 출력

```
############ assembleDebug ############
BUILD SUCCESSFUL in 5s          (^e: 0건, ^w: 0건)

############ testDebugUnitTest --rerun-tasks ############
BUILD SUCCESSFUL in 5s
  BridgeProtocolTest:    tests=10 failures=0 skipped=0
  AppInfoRepositoryTest: tests=3  failures=0 skipped=0
  MainViewModelTest:     tests=12 failures=0 skipped=0
  BridgeDispatcherTest:  tests=6  failures=0 skipped=0
  합계: 31건, 실패 0, skip 0

############ compileDebugKotlin --rerun-tasks ############
경고(^w:) 0건

############ jvmCoverageReport --rerun-tasks ############
BUILD SUCCESSFUL
  LINE:   98/98 = 100.0%
  BRANCH: 16/18 = 88.9%
  70% 미달 클래스: 없음
```

**커버리지 회귀 없음** — 리뷰어가 §9-8에서 베이스라인으로 제시한 **LINE 98/98(100.0%) / BRANCH 16/18(88.9%)와 정확히 동일**하다. (MINOR-2로 추가한 `catch (CancellationException)`는 `NativeBridgeHandler`가 커버리지 제외 대상이라 분모에 들어가지 않는다.)

### V2 — 수정한 테스트가 여전히 실패를 잡는지 재증명
MINOR-5에서 Fake의 의미를 바꿨으므로 그 테스트를 다시 RED로 만들어 확인했다.

| 단계 | 조작 | 결과 |
|:--|:--|:--|
| RED | `BridgeDispatcher`가 Repository 예외를 `runCatching{}.getOrDefault("SWALLOWED")`로 **삼키도록** 일시 변경 | `BridgeDispatcherTest > Repository 예외는 삼켜지지 않고 그대로 전파된다` **FAILED** |
| 원복 | 백업본 복원 (`grep -c SWALLOWED` = **0**) | **BUILD SUCCESSFUL** |

### 실기기 회귀 — 브리지 경로를 건드렸으므로(MINOR-2) 재확인
기기 `R3CN60L0QMT`(API 33), 재빌드·재설치 후:

| 항목 | 관측값 |
|:--|:--|
| 확장 페이지 + JS 실행 | `text="READY"` |
| **3단 왕복**(MINOR-2가 지나는 경로) | 화면 `versionName = 1.0.0` ↔ `dumpsys versionName=1.0.0` **문자 단위 일치** |
| **5단 경로**(외부 사이트 페이지 세계) | naver.com 배지 `versionName = 1.0.0 [PAGE_WORLD]` |
| 치명 오류 | `FATAL EXCEPTION`/`Fatal signal`/`libc: Fatal` **0건** |

### 화이트리스트 / 잔존물
- **수정한 7개 파일 전부 화이트리스트 안**(`.gitignore`·`app/build.gradle.kts`는 `allowed_files`, 나머지 5개는 글롭). 막힌 시도 **없음**.
- 임시 코드 잔존 재검색(`SWALLOWED|7.7.7-probe|10.255.255.1|AC-010-5 검증용|TODO|FIXME`): **0건**.
- 비행기모드 `airplane_mode_on = 0`.

---

## 6. 수정한 파일 목록

| 파일 | 지적 | 성격 |
|:--|:--|:--|
| `app/src/main/java/com/example/geckoviewtest/MainActivity.kt` | **MAJOR-1** + MINOR-4 | 주석만 |
| `app/src/main/java/com/example/geckoviewtest/gecko/AppNavigationDelegate.kt` | **MAJOR-1** | 주석만 |
| `app/build.gradle.kts` | MINOR-6 | 주석만 |
| `app/src/main/java/com/example/geckoviewtest/App.kt` | MINOR-9 | 주석만 |
| `app/src/main/java/com/example/geckoviewtest/bridge/NativeBridgeHandler.kt` | MINOR-2 | **코드 변경**(catch 절 추가 + import) |
| `app/src/test/java/com/example/geckoviewtest/bridge/BridgeDispatcherTest.kt` | MINOR-5 | **테스트 변경**(이름 + Fake 시그니처) |
| `.gitignore` | MINOR-7 | 항목 추가 |

**프로덕션 로직 변경은 MINOR-2 한 곳뿐**이며 나머지는 주석·테스트·gitignore다. 아키텍처 결정(D-01~D-13), 의존성, 자산(JS/HTML), 브리지 계약은 **한 줄도 바뀌지 않았다.**

---

## 7. code-reviewer 재리뷰 인계 사항

1. **MAJOR-1 해소.** 두 주석 모두 요구된 3요소 + 초보자용 풀이 + `(AC-011-3)` 정정 대기 표기를 담았다. **API 30 이하 동작은 실측하지 못했으므로 주석에 그 한계를 명시**했다(없는 관측을 주장하지 않았다).
2. **MINOR-6 처리.** `app/build.gradle.kts`의 activity-ktx 선언 위. `libs.versions.toml`이 아니라 이쪽을 고른 이유는 **`exclude`를 쓰려는 사람이 실제로 손대는 파일이 dependencies 블록**이기 때문이다.
3. **새로 검증이 필요한 변경은 MINOR-2 하나**다(프로덕션 로직). 단위 테스트로는 덮이지 않는다 — `NativeBridgeHandler`는 GeckoView 의존이라 커버리지 제외 대상이다. **실기기 3단·5단 왕복 회귀로 확인**했고 결과는 §5에 있다.
4. **리뷰어가 지적한 계수 착오 인정.** 직전 로그의 "소스 40개"는 **39개**가 맞다(섹션 라벨 "빌드/설정 (9)"→실제 10, "Kotlin (11)"→실제 12). 결론(위반 0건)에는 영향이 없다. 이번 로그에서는 파일 수를 단정하지 않고 **수정 파일 7개만 열거**했다.
5. **MINOR-1·MINOR-8은 의도적으로 손대지 않았다**(§4). 리뷰어 권고와 V9에 따른 것이며, 각각 다음 배치 요구사항 / release 도입 선행 작업으로 기록했다.
6. **planner-analyzer 소관 잔여 작업**: `requirements.md`의 AC-011-3·AC-002-2 문언 정정. 코드 주석은 이미 정정된 사실을 담고 있으므로 **문서가 코드를 따라오면 된다.**
