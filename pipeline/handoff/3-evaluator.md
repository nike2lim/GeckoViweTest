# evaluator 핸드오프
- **판정**: **APPROVED (조건부 — C-1·C-2)**
- **전문**: `pipeline/evaluation.md`
- **로그**: `doc/20260810/20260810_152226_evaluator.md`

## 다음 단계가 반드시 알아야 할 것

1. **developer는 착수해도 된다.** 조건 2건은 계획의 구조·결정·Step 순서를 **바꾸지 않는다.** plan.md 수정 없이 이행하고 로그에 남겨라.
2. **C-1 — `G2-a`의 "컴파일 경고에 unused import 0건"을 근거로 쓰지 마라. 공허한 조건이다.** Kotlin 2.3.21은 미사용 import에 경고를 내지 않는다(같은 버전으로 실측: **경고 0건, BUILD SUCCESSFUL**). 대신 **`git diff App.kt`의 import 블록을 plan §4 Step 2가 열거한 삭제/잔존 목록과 1:1 대조**하라 — 그쪽이 더 강한 검사다.
3. **C-2 — `G4-a`의 RED 증명을 케이스 2에도 하라.** 계획의 예시(`requestFinish()` 주석 처리)는 케이스 1만 RED로 만든다. 케이스 2(null 분기)는 `onFinishRequested?.invoke()`를 **`!!`로 바꿔 NPE가 나는 것**으로 증명한다(원복 기록 포함). **R-P6(`?.`→`!!`)과 R-P7을 동시에 방어**한다.
4. **목표 102/102 = 100.00%는 성립한다 — 독립 재계산 확인. 하락 지표 0건.**
5. **`bridge/AppBridgeHost.class`가 분모에 들어오는 것은 확정 사실이다**(제외 목록 11개 원문을 스크래치패드 Gradle에서 실행 확인 — Ant `*`는 `/`를 안 넘음). **G1-d는 깨질 이유가 없다.** 깨지면 §7대로 넓히지 말고 즉시 반환하라.
6. **qa(6단계)는 승인 도장이 아니다.** Step 4를 developer가 쓰더라도 qa는 **케이스 추가 권한을 유지한다**(넓히라는 뜻은 아니다 — 102/102에 추가 케이스는 불요).

## 확정된 결정

- **E-1 `AppBridgeHost` → `bridge/` : APPROVED.** 새 의존 간선 **0개**(포트 `interface BridgeHost`가 같은 패키지가 되어 import가 오히려 사라진다). `bridge`/`data`의 루트 참조 **0건** — 순환·역방향 없음.
- **E-2 `AppContainer` 루트 유지 : APPROVED. 사용자 의도를 임의로 좁힌 것이 아니다.** 불만의 지시 대상이 "**App Class에 있는**"이라 App.kt 밖으로 나가면 요청 충족이고, `App.kt:96-98`에 "Hilt를 안 쓴다"가 이미 문서화돼 클래스 1개짜리 `di/`는 그와 어긋난다. **단 "80.33% 회귀" 논증은 조건부다**(`di/`면 패턴 한 줄 고치면 그만 — planner도 스스로 격하). 근거 1·2가 결론을 지탱한다. 상세 §1-E2.
- **E-3 Step 4를 developer가 수행 : APPROVED — 역할 위반이 아니다.** 규칙에 금지 조항이 없다(qa 쪽에만 "프로덕션 코드 수정 금지" 제약이 있고 developer 쪽엔 대칭 제약이 없다). 대안(qa가 6단계 + code-reviewer가 96.08% 무시)은 **게이트에게 자기 측정치를 무시하라고 가르치는 구조**라 기각.
- **E-4 제외 유지 : APPROVED**(목록 **11 → 10으로 좁아짐**, 확대 아님). **E-5 주석만 갱신 : APPROVED**(전수 grep 누락 0건).
- **`architecture.md` 전 조항 PASS.** 커버리지 **두 조항 모두 충족**. 새 라이브러리 0건, 빌드 설정 변경은 `coverageExclusions` 1줄 삭제 + 주석뿐.
- **화이트리스트 이탈 0건**(hook과 같은 `fnmatch`로 6개 경로 재실행) → **impact-analyzer 재실행 불필요. 범위 확대 0건.**

## 재측정 (V7) — **실질 불일치 없음**

출처: 방금 실행한 `clean jvmCoverageReport --rerun-tasks`의 XML(planner 값과 전부 일치):
- 베이스라인 **LINE 98/98, BRANCH 18/18, CLASS 14/14, INSTR 607/636, METHOD 45/55, CXTY 54/64**
- `AppBridgeHost` **LINE 4 / BRANCH 2 / METHOD 4 / INSTR 19 / CXTY 5 / CLASS 1**(planner와 **다른 측정 경로**인데 완전 일치)
- 테스트 **48건 skipped 0** / `grep -c '^class ' App.kt` = **3** / 제외 항목 **11개** / 실기기 **SM-G981N 연결**
- **JDK 21(21.0.10)도 BUILD SUCCESSFUL, 6개 지표 동일** — **21로 잡혔다고 그게 실패 원인일 수 없으니 다른 원인을 찾아라.**
- 사소한 것(판정 영향 없음): **`App.kt`는 168줄**(계획의 "169줄"은 끝 빈 줄 차이). **R-P3는 재현 안 했다** — `clean`은 무조건 안전하니 **그대로 붙여라.**

## 전문을 열어야 하는 경우

- **developer**: 착수 전 **§6**(주의 9항목). C-1·C-2 문언은 **§판정 표**.
- **code-reviewer**: **§3 V1 게이트 표**. 96.08%는 plan §3.4대로 **회귀 아님.**
- **qa**: **§6-3**(케이스 추가 권한·한계).
- **오케스트레이터**: **§1-E2c** — E-2의 남는 해석 여지와 **전환 비용**(사용자가 `di/`를 원하면 파이프라인 재실행 없이 후속 배치로 가능). **4단계와 병렬 확인 가능.**

## 미해결 / 이월

- **F-2** — 이 배치에서 닫힌다(Step 3+4 짝). **F-3**·**F-4** 이월.
- **F-1**(assets `inputs.dir`) **범위 밖.** Step 3에 "추가 금지"가 있고 G3-a가 검출한다. **손대지 마라.**
- **`interface BridgeHost` 분리**(`BridgeDispatcher.kt:59`)는 화이트리스트 밖 — **후속 후보로만. 넓히지 마라.**
